import styled from 'styled-components';

interface ChoiceBoxProps {
  options: string[];
  bgColor?: string;
  textColor?: string;
}

const ChoiceBox: React.FC<ChoiceBoxProps> = ({options, bgColor, textColor}) => {
  return (
    <div>
      {/* <BoxContainer>

      </BoxContainer> */}
    </div>
  );
};
