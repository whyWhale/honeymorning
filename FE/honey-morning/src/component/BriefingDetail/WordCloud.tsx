import styled from 'styled-components';

const WordCloud = () => {
  return <Container>WordCloud</Container>;
};

const Container = styled.div`
  display: flex;

  width: 100%;
  height: 50rem;
  border: 1px solid lime;
`;

export default WordCloud;
