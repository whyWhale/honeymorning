spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

api:
  key:
    elevenlabs: ${ELEVEN_LABS_API_KEY:test}

logging:
  level:
    root: debug